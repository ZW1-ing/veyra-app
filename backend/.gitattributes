# 统一换行符：代码在仓库和 Linux 容器里一律用 LF，
# Windows 上的 PowerShell 脚本保留 CRLF，避免执行时报错。
* text=auto eol=lf

*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不要做换行转换
*.png binary
*.jpg binary
*.ico binary
