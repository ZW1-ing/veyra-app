# Web 框架与服务器
fastapi>=0.115
uvicorn[standard]>=0.32

# 数据层：SQLAlchemy 2.x ORM + MySQL 驱动
SQLAlchemy>=2.0
PyMySQL>=1.1
cryptography>=42.0          # MySQL 8/9 的 caching_sha2_password 认证需要
alembic>=1.13

# 配置与 HTTP
pydantic-settings>=2.5
httpx>=0.27
python-dotenv>=1.0

# 文档解析（知识库上传 PDF / DOCX）
pypdf>=5.0
python-docx>=1.1
python-multipart>=0.0.9      # FastAPI 处理 multipart 上传

# Agent 编排
langgraph>=0.2
langchain-core>=0.3

# 测试
pytest>=8.3
pytest-asyncio>=0.24
asgi-lifespan>=2.1
# 仅测试用：在测试里现场生成一个真实 PDF，验证解析链路而不是打桩
reportlab>=4.0

# 代码检查（开发用）
ruff>=0.6
